# Redirect all traffic to index.html for SPA routing
/*    /index.html   200

# API routes (uncomment and modify if you need to proxy to a backend)
/api/*  https://your-backend-url.com/api/:splat  200
